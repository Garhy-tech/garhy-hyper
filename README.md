# GARHY | HYPER

A full-stack e-commerce storefront and admin dashboard built on **TanStack Start**
(React 19 SSR). Bilingual (Arabic / English, with RTL support), backed by Supabase
for auth and data.

## Tech stack

| Concern        | Choice                                                  |
| -------------- | ------------------------------------------------------- |
| Framework      | TanStack Start + TanStack Router (file-based routing)   |
| UI runtime     | React 19 (server-side rendering)                        |
| Server bundle  | Nitro (`node-server` preset → `.output/server/index.mjs`) |
| Data / auth    | Supabase (`@supabase/ssr`, PostgREST)                   |
| Data fetching  | TanStack Query + TanStack Start server functions        |
| Styling        | Tailwind CSS v4 (`@tailwindcss/vite`) + Lightning CSS   |
| i18n           | i18next / react-i18next (AR + EN, RTL aware)            |
| Animation      | Framer Motion                                           |
| Icons          | lucide-react                                            |
| Toasts         | sonner                                                  |
| Language / PM  | TypeScript, Node.js ≥ 20, npm ≥ 10                      |

## Getting started

```bash
# 1. Install dependencies
npm install

# 2. Configure environment
cp .env.example .env
#   then fill in your Supabase values (see docs/ENVIRONMENT.md)

# 3. Start the dev server (http://0.0.0.0:5000)
npm run dev
```

## Scripts

| Script              | Description                                            |
| ------------------- | ------------------------------------------------------ |
| `npm run dev`       | Start the Vite dev server on `0.0.0.0:5000`            |
| `npm run build`     | Production build (emits `.output/server/index.mjs`)    |
| `npm run start`     | Run the built production server (`node .output/...`)   |
| `npm run preview`   | Preview the production build locally                   |
| `npm run lint`      | ESLint over `src`, `vite.config.ts`, `eslint.config.js` |
| `npm run format`    | Prettier write over the same paths                     |

Type-check with `npx tsc --noEmit`.

## Project structure

```
src/
├── routes/                 # File-based routes (see src/routes/README.md)
│   ├── __root.tsx          # App shell: providers, <head>, i18n, <Outlet />
│   ├── index.tsx           # Home
│   ├── catalog.*.tsx       # Catalog / category / product browsing
│   ├── cart.tsx, checkout.tsx, wishlist.tsx, search.tsx, auth.tsx ...
│   └── _authenticated/     # Pathless auth-guarded layout
│       ├── route.tsx       # Supabase getUser() guard → redirect to /auth
│       ├── account.*.tsx   # Customer account area
│       └── admin.*.tsx     # Admin dashboard
├── components/
│   ├── ui/                 # Minimal shadcn-style primitives actually in use
│   ├── commerce/ home/ layout/ common/ admin/ motion/
├── contexts/               # React context providers
├── hooks/                  # use-language, etc.
├── integrations/supabase/  # Browser + server clients, auth middleware
├── lib/
│   ├── api/                # Server functions (createServerFn) + PostgREST helpers
│   ├── locales/            # AR / EN translation resources
│   ├── i18n.ts             # i18next init
│   └── config.server.ts    # Server-only config accessor
├── router.tsx              # Router + QueryClient wiring
├── server.ts               # SSR server entry (error boundary)
└── start.ts                # Function + request middleware registration
```

`src/routeTree.gen.ts` is generated by TanStack Router — **do not edit by hand**,
and do not rename or move files under `src/routes/` (it breaks file-based routing).

## Documentation

- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — how the pieces fit together.
- [docs/ENVIRONMENT.md](docs/ENVIRONMENT.md) — environment variables, secrets, deployment.
- [src/routes/README.md](src/routes/README.md) — file-based routing conventions.

## Database & empty states

The app reads catalog data from Supabase via PostgREST. If the database schema
has not been migrated or tables are empty, the data layer **fails gracefully** —
PostgREST 404/406 responses are caught and surfaced as empty arrays, so the UI
renders localized empty states and loading skeletons rather than crashing.
Applying migrations and seeding data is handled separately (outside this app).

## Deployment

Build, then run the standalone Node server:

```bash
npm run build
npm run start   # node .output/server/index.mjs
```

Provide the four Supabase environment variables in the production environment.
See [docs/ENVIRONMENT.md](docs/ENVIRONMENT.md).
